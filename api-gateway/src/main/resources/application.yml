management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  otlp:
    metrics:
      export:
        url: http://localhost:4318/v1/metrics
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/traces
  logging:
    export:
      otlp:
        endpoint: http://localhost:4318/v1/logs
  endpoint:
    health:
      show-details: always
  tracing:
    sampling:
      probability: '1.0'
spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: product-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/product/**

            - id: order-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/order/**

            - id: inventory-service
              uri: http://localhost:8083
              predicates:
                - Path=/api/inventory/**,/api/category/**,/api/promotion/**

  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8181/realms/techmarket-realm
logging:
  pattern:
    level: "%5p [traceId=%X{traceId:-}, spanId=%X{spanId:-}]"
  level:
    org:
      springframework:
        boot:
          actuator:
            autoconfigure:
              opentelemetry: DEBUG
          docker:
            compose: DEBUG
        cloud:
          gateway:
            route:
              RouteDefinitionLocator: INFO
            nodeValue: TRACE
    root: INFO
    io.micrometer: DEBUG
    io.opentelemetry: DEBUG
    org.springframework.boot.actuate: DEBUG
server:
  port: '8080'
  forward-headers-strategy: framework
